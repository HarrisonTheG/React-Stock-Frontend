import { Typography, Box } from '@material-ui/core'
import React from 'react'

const SearchStock = () => {
    return (
        <Box>
            <Box sx={{height: '80px'}}></Box>
            <Typography>Search Stock page</Typography>
        </Box>
    );
}

export default SearchStock
